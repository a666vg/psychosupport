# psychology_bot 🧠
# оригинальный проект - https://github.com/frolovelo/saloon_bot

**psychology_bot** — Telegram-бот для записи на консультации к психологам с использованием Google Sheets.

![Static Badge](https://img.shields.io/badge/python-3.11-blue)
![Static Badge](https://img.shields.io/badge/pyTelegramBotAPI-4.12.0-blue)
![Static Badge](https://img.shields.io/badge/gspread-5.10.0-blue)

## Описание

**psychology_bot** — это Telegram-бот, который позволяет пользователям:
- Выбирать локацию и психолога для консультации.
- Просматривать доступные даты и время для записи.
- Подтверждать или отменять записи.
- Просматривать свои актуальные записи.

Бот интегрируется с **Google Sheets** для хранения расписания и записей клиентов. Он использует интерактивный календарь для выбора дат и поддерживает кэширование для оптимизации запросов к API Google Sheets.

**Пример таблицы Google Sheets**: [Создайте таблицу по шаблону](#структура-google-sheets)

**Документация библиотек**:
- [pyTelegramBotAPI](https://github.com/eternnoir/pyTelegramBotAPI)
- [gspread](https://docs.gspread.org/en/v5.7.2/)

## Возможности

- **Запись к психологу**: Выбор локации, психолога, даты и времени через удобный интерфейс Telegram.
- **Отмена записи**: Возможность отменить существующую запись.
- **Просмотр записей**: Отображение всех актуальных записей пользователя.
- **Кэширование**: Минимизация запросов к Google Sheets API с помощью кэша (TTL: 12 часов для листов, 15 минут для дат).
- **Многопоточность**: Использование `ThreadPoolExecutor` для параллельной обработки листов.
- **Логирование**: Подробные логи для отладки и мониторинга.

## Установка

### Требования
- Python 3.11
- Telegram-бот токен (получите через [BotFather](https://t.me/BotFather))
- Google Sheets API JSON-ключ (см. [Настройка Google Sheets API](#настройка-google-sheets-api))
- Доступ к Google Sheets таблице с правильной структурой

### Клонирование репозитория
```bash
git clone <your_repository_url>
cd psychology_bot
```

### Установка зависимостей
Создайте виртуальное окружение и установите зависимости:

**Windows**:
```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

**macOS/Linux**:
```bash
python3 -m venv venv
source venv/bin/activate
pip3 install -r requirements.txt
```

### Зависимости
См. [requirements.txt](requirements.txt):
- `pyTelegramBotAPI==4.12.0`
- `gspread==5.10.0`
- `cachetools`
- `retrying`
- `google-auth`

## Настройка

### 1. Настройка Telegram-бота
1. Создайте бота через [BotFather](https://t.me/BotFather):
   - Отправьте команду `/newbot`.
   - Следуйте инструкциям, чтобы получить токен (например, `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`).
2. Создайте файл `config.py` в корне проекта:
   ```python
   TOKEN = "YOUR_BOT_TOKEN"
   ```
   Замените `YOUR_BOT_TOKEN` на полученный токен.

### 2. Настройка Google Sheets API
1. **Создайте проект в Google Cloud Console**:
   - Перейдите на [Google Cloud Console](https://console.cloud.google.com).
   - Нажмите **New Project**, задайте имя (например, `PsychologyBot`), и создайте проект.
2. **Включите Google Sheets API**:
   - В меню **APIs & Services** > **Library** найдите `Google Sheets API` и нажмите **Enable**.
3. **Создайте сервисный аккаунт**:
   - В **APIs & Services** > **Credentials** нажмите **Create Credentials** > **Service Account**.
   - Задайте имя (например, `psychology-bot-service-account`), выберите роль **Editor**, и создайте.
4. **Получите JSON-ключ**:
   - В разделе **Keys** для сервисного аккаунта нажмите **Add Key** > **Create new key** > **JSON**.
   - Скачанный файл (например, `psychology-bot-123.json`) переименуйте в `psychology.json` и поместите в корень проекта.
5. **Обновите google_sheet.py**:
   Убедитесь, что в `google_sheet.py` указано правильное имя JSON-файла:
   ```python
   creds = Credentials.from_service_account_file('psychology.json', scopes=myscope)
   ```

### 3. Настройка Google Sheets
1. **Создайте таблицу**:
   - Перейдите в [Google Sheets](https://sheets.google.com) и создайте новую таблицу.
   - Скопируйте ID таблицы из URL (например, `https://docs.google.com/spreadsheets/d/YOUR_TABLE_ID/edit`).
2. **Обновите google_sheet.py**:
   Укажите ID вашей таблицы:
   ```python
   sh = client_main.open_by_key('YOUR_TABLE_ID')
   ```
3. **Настройте структуру таблицы**:
   См. [Структура Google Sheets](#структура-google-sheets).
4. **Предоставьте доступ**:
   - Нажмите **Share** в Google Sheets.
   - Добавьте email сервисного аккаунта (из JSON-ключа, например, `psychology-bot-service-account@your-project-id.iam.gserviceaccount.com`) с правами **Editor**.

## Структура Google Sheets

Таблица должна содержать следующие листы:

### Лист "Психологи"
- **Колонки**:
  - `Локация`: Адрес консультации (например, `ул. Б. Семеновская 36`).
  - `Психолог`: Имя психолога (например, `Сафронова Ольга`).
- **Пример**:
  ```
  | Локация                    | Психолог             |
  |----------------------------|----------------------|
  | ул. Б. Семеновская 36      | Сафронова Ольга      |
  | ул. Павла Корчагина 22     | Абасова Лейла        |
  | ул. Прянишникова 2А        | Орлова Виктория      |
  | ул. Прянишникова 2А        | Пузий Анастасия      |
  | ул. Автозаводская 16       | Расулова Марьям      |
  ```

### Листы с датами
- **Название листа**: Дата в формате `дд.мм.гггг` (например, `10.05.2025`).
- **Колонки**:
  - `Время`: Время в формате `ЧЧ:ММ` (например, `10:00`).
  - `Психолог`: Имя психолога, соответствующее данным в листе "Психологи".
  - `Клиент`: Пусто для свободных слотов или строка с данными клиента (например, `id:123 @username tel:+79991234567`).
- **Пример листа `10.05.2025`**:
  ```
  | Время  | Психолог           | Клиент                              |
  |--------|--------------------|-------------------------------------|
  | 10:00  | Сафронова Ольга    |                                     |
  | 11:00  | Сафронова Ольга    | id:467168798 @user tel:+79522600066 |
  | 12:00  | Абасова Лейла      |                                     |
  | 13:00  | Абасова Лейла      |                                     |
  ```

### Требования
- Названия листов с датами должны быть в формате `дд.мм.гггг`.
- Колонки в листах с датами: `Время`, `Психолог`, `Клиент` (в этом порядке).
- Лист "Психологи" обязателен.
- Пустая ячейка в колонке `Клиент` означает свободный слот.
- Время должно быть в формате `ЧЧ:ММ` и соответствовать часовому поясу `Europe/Moscow`.

## Использование

1. **Запустите бот**:
   ```bash
   python main.py
   ```
2. **Взаимодействуйте с ботом**:
   - Откройте Telegram и найдите ваш бот (например, `@PsychologyBot`).
   - Нажмите `/start`.
   - Отправьте номер телефона, если требуется.
   - Используйте главное меню для:
     - **Запись**: Выберите локацию, психолога, дату и время.
     - **Отмена записи**: Выберите запись для отмены.
     - **Мои записи**: Просмотрите текущие записи.

## Структура проекта

- **[config.py](config.py)**: Токен Telegram-бота.
- **[main.py](main.py)**: Основная логика Telegram-бота, обработка команд и callback-запросов.
- **[google_sheet.py](google_sheet.py)**: Взаимодействие с Google Sheets (чтение/запись данных, кэширование).
- **[clear_dict.py](clear_dict.py)**: Управление временными данными клиентов и очистка неиспользуемых данных.
- **[keyboards.py](keyboards.py)**: Создание клавиатур Telegram (меню, кнопки).
- **[telebot_calendar.py](telebot_calendar.py)**: Интерактивный календарь для выбора дат.
- **[requirements.txt](requirements.txt)**: Список зависимостей.

## Отладка и устранение неполадок

### Бот зависает при нажатии "Запись"
- **Проверьте структуру таблицы**:
  - Убедитесь, что листы с датами названы в формате `дд.мм.гггг`.
  - Проверьте наличие листа "Психологи" и правильность колонок.
- **Проверьте доступ к Google Sheets**:
  - Убедитесь, что сервисный аккаунт имеет права **Editor**.
  - Проверьте ID таблицы в `google_sheet.py`.
- **Проверьте логи**:
  - Логи выводятся в консоль (см. `google_sheet.py` и `main.py`).
  - Ищите ошибки вроде `Worksheet not found` или `Error parsing date`.
- **API квоты**:
  - Если возникает ошибка `Quota exceeded`, уменьшите частоту запросов или увеличьте квоту в Google Cloud Console.

### Ошибка "Permission denied"
- Проверьте, добавлен ли email сервисного аккаунта в список доступа таблицы.
- Убедитесь, что Google Sheets API включен в Google Cloud Console.

### Ошибка "File not found"
- Проверьте, что `psychology.json` находится в корне проекта и имя указано правильно в `google_sheet.py`.

## Разработка и вклад

### TO-DO
- Перейти на базу данных (SQLAlchemy/MongoDB) для хранения номеров телефонов.
- Добавить удаление устаревших дат из Google Sheets.
- Реализовать асинхронную обработку с `aiogram` для повышения производительности.
- Добавить уведомления о записях (например, за день до консультации).
- Создать админ-бота для управления расписанием и настройками.
- Добавить анимацию загрузки для улучшения UX.

### Внесение изменений
1. Создайте форк репозитория.
2. Внесите изменения и протестируйте с тестовой таблицей.
3. Создайте Pull Request с описанием изменений.

### Референсы
- [purgy/telebot-calendar](https://github.com/purgy/telebot-calendar): Основа для интерактивного календаря.

## Лицензия

MIT License. См. [LICENSE](LICENSE) для деталей.

## Контакты

Для вопросов и предложений создавайте Issues в репозитории или свяжитесь с разработчиком через Telegram.